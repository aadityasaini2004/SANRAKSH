package om.sanraksh.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Primary (Trust, Safety, Calm) ──
val Primary = Color(0xFF0D9488)
val PrimaryDark = Color(0xFF0F766E)
val PrimaryLight = Color(0xFFCCFBF1)
val OnPrimary = Color(0xFFFFFFFF)
val OnPrimaryContainer = Color(0xFF002020)

// ── Emergency ──
val Emergency = Color(0xFFDC2626)
val EmergencyDark = Color(0xFFB91C1C)
val EmergencyLight = Color(0xFFFEE2E2)
val OnEmergency = Color(0xFFFFFFFF)

// ── Success ──
val Success = Color(0xFF16A34A)
val SuccessDark = Color(0xFF15803D)
val SuccessLight = Color(0xFFDCFCE7)
val OnSuccess = Color(0xFFFFFFFF)

// ── Warning ──
val Warning = Color(0xFFD97706)
val WarningLight = Color(0xFFFEF3C7)
val OnWarning = Color(0xFFFFFFFF)

// ── Surfaces ──
val Background = Color(0xFFFAFAF9)
val Surface = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFF5F5F4)
val OnBackground = Color(0xFF1C1917)
val OnSurface = Color(0xFF1C1917)
val OnSurfaceVariant = Color(0xFF57534E)

// ── Outline ──
val Outline = Color(0xFFD6D3D1)
val OutlineVariant = Color(0xFFE7E5E4)

// ── Error (form validation) ──
val ErrorRed = Color(0xFFDC2626)
val ErrorRedLight = Color(0xFFFEE2E2)
val OnError = Color(0xFFFFFFFF)